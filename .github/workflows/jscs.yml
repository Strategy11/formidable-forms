name: Inspections

on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, labeled, synchronize]

jobs:
  runESLintInspection:
    if: contains(github.event.pull_request.labels.*.name, 'run analysis')
    name: Run ESLint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "20.10.0"
          cache: "npm"
          cache-dependency-path: package-lock.json
          # Ensure caching is enabled even if packageManager field is missing
          package-manager-cache: true

      - name: Debug - Package-lock.json analysis
        run: |
          echo "📋 PACKAGE-LOCK.JSON ANALYSIS:"
          echo "File exists: $([ -f package-lock.json ] && echo 'YES' || echo 'NO')"
          if [ -f package-lock.json ]; then
            echo "File size: $(stat -c%s package-lock.json) bytes"
            echo "Last modified: $(stat -c%y package-lock.json)"
            echo "File hash: ${{ hashFiles('**/package-lock.json') }}"
          fi
          echo "Generated cache key: ${{ runner.os }}-node-modules-${{ hashFiles('**/package-lock.json') }}"
          echo ""
          echo "🔍 CACHE LOGIC: Any change to package-lock.json → Different hash → Cache miss → npm ci runs"

      - name: Cache node_modules
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-${{ hashFiles('**/package-lock.json') }}
            ${{ runner.os }}-node-modules-
          # Allow restore-keys to find caches across PR pushes

      - name: Debug - Cache result
        run: |
          echo "🎯 CACHE DECISION:"
          echo "Cache hit: ${{ steps.npm-cache.outputs.cache-hit }}"
          if [ "${{ steps.npm-cache.outputs.cache-hit }}" = "true" ]; then
            echo "✅ CACHE HIT - package-lock.json UNCHANGED - Dependencies will be SKIPPED"
            echo "   → This means NO changes to dependencies, versions, or new packages"
          else
            echo "❌ CACHE MISS - package-lock.json CHANGED - Dependencies will be INSTALLED"
            echo "   → This means: new dependencies, version updates, or dependency changes detected"
            echo "   → npm ci --include=dev will run to ensure correct dependencies"
          fi

      - name: Debug - Pre-install state
        run: |
          echo "Node modules exists: $([ -d node_modules ] && echo 'YES' || echo 'NO')"
          if [ -d node_modules ]; then
            echo "Node modules count: $(find node_modules -maxdepth 1 -type d | wc -l)"
          fi

      - name: Check if installation needed
        id: check-install
        run: |
          if [ "${{ steps.npm-cache.outputs.cache-hit }}" = "true" ]; then
            echo "needs-install=false" >> $GITHUB_OUTPUT
            echo "reason=exact-cache-hit" >> $GITHUB_OUTPUT
          elif [ -d "node_modules" ] && [ -f "./node_modules/.bin/eslint" ]; then
            echo "needs-install=false" >> $GITHUB_OUTPUT  
            echo "reason=cache-restored" >> $GITHUB_OUTPUT
          else
            echo "needs-install=true" >> $GITHUB_OUTPUT
            echo "reason=no-cache-or-missing-deps" >> $GITHUB_OUTPUT
          fi

      - name: Install dependencies
        if: steps.check-install.outputs.needs-install == 'true'
        run: |
          echo "🔄 INSTALLING DEPENDENCIES (cache miss detected)"
          echo "📝 Reasons this could happen:"
          echo "   • New dependency added to package.json"
          echo "   • Dependency version updated" 
          echo "   • package-lock.json manually changed"
          echo "   • First run or different branch"
          npm ci --include=dev
          echo "✅ INSTALLATION COMPLETED - All dependencies are now correct"

      - name: Debug - Installation decision proof
        run: |
          echo "🔍 FINAL DECISION:"
          echo "Reason: ${{ steps.check-install.outputs.reason }}"
          if [ "${{ steps.check-install.outputs.needs-install }}" = "true" ]; then
            echo "✅ RAN INSTALLATION - ${{ steps.check-install.outputs.reason }}"
          else
            echo "✅ SKIPPED INSTALLATION - ${{ steps.check-install.outputs.reason }}"
          fi

      - name: Debug - Post-install state
        run: |
          echo "Final node modules count: $(find node_modules -maxdepth 1 -type d | wc -l)"
          echo "ESLint executable exists: $([ -f ./node_modules/.bin/eslint ] && echo 'YES' || echo 'NO')"

      - name: Analyze js code style
        run: ./node_modules/.bin/eslint .
